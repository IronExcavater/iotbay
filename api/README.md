# IoTBay API

Flask backend for the IoTBay marketplace. The API is managed with `uv`, stores
data in SQLite, and exposes routes under `/api`.

## Local Commands

```bash
npm run -w api sync
npm run -w api dev
npm run -w api check:all
npm run -w api test
```
