{
    "name": "iotbay-api",
    "description": "Flask backend for IoTBay",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "sync": "uv sync && npm run migrate && npm run seed",
        "dev": "uv run python -m flask --app run:app --debug run --host 0.0.0.0 --port 5001",
        "db": "uv run python -m src.db",
        "migrate": "npm run db -- migrate",
        "migrate:new": "npm run db -- migrate-new",
        "seed": "npm run db -- seed-load",
        "seed:dump": "npm run db -- seed-dump",
        "typecheck": "uv run pyright",
        "ruff": "uv run ruff check . && uv run ruff format . --check",
        "ruff:fix": "uv run ruff check . --fix && uv run ruff format .",
        "check:all": "npm run typecheck && npm run ruff",
        "fix:all": "npm run typecheck && npm run ruff:fix",
        "test": "npm run test:unit && npm run test:api",
        "test:unit": "uv run python -m unittest discover -s tests",
        "test:api": "uv run python -m unittest discover -s acceptance",
        "test:e2e": "uv run python -m unittest discover -s e2e",
        "build": "uv build"
    }
}
